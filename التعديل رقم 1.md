# التعديل رقم 1 — ربط مشروع الويب (Pharma Manager) بقاعدة بيانات MedLink Supabase

> **التاريخ:** 2026-08-08
> **المشروع:** `Medlik-Waap` (لوحة تحكم المدير العام — Web PWA)
> **قاعدة البيانات:** Supabase `lmkomzqioneuyvatzsov` (نفس قاعدة بيانات مشروع Flutter)
> **المستودع المحلي:** `C:\Users\USER\Medlik-Waap`

---

## 1. القرارات المعمارية المطبَّقة

1. **الويب حصري لدور `company_director`** — أي محاولة دخول من دور آخر (branch_manager / driver / client) تُرفض فوراً ويُسجَّل الخروج تلقائياً.
2. **حذف كامل لواجهة مدير الفرع من الويب** — لا ازدواجية؛ مدير الفرع له واجهته فقط في تطبيق Flutter.
3. **لا بيانات وهمية** — أُزيلت كل أوضاع المعاينة (preview mode) والأكواد التجريبية.
4. **الكتابة على جدول المستخدمين تمر حصراً عبر Edge Function** (بنمط `manage-driver-account` الآمن) — لا إدخال يدوي في `public.users`.

---

## 2. التغييرات التي تمت (المرحلة 1 — التأسيس)

### 2.1 الاتصال بقاعدة البيانات
- **`artifacts/pharma-pwa/.env.local`** (جديد، متجاهَل بـ git):
  ```
  VITE_SUPABASE_URL=https://lmkomzqioneuyvatzsov.supabase.co
  VITE_SUPABASE_ANON_KEY=<ضع المفتاح من .env.local>  # لا يُرفع للمستودع
  PORT=5173
  BASE_PATH=/
  ```
- **`.gitignore`** — أُضيفت `.env` / `.env.local` / `.env.*.local` لمنع تسريب المفاتيح.

### 2.2 عميل Supabase
- **`src/lib/supabaseClient.ts`** — أُعيدت كتابته: قراءة المفاتيح من المتغيرات البيئية مع **فشل صريح** برسالة واضحة إذا كانت ناقصة (بدلاً من القيم الوهمية السابقة لـ Firebase).

### 2.3 المصادقة وحارس الدور
- **`src/contexts/AuthContext.tsx`** — أُعيدت كتابته بالكامل:
  - `ALLOWED_ROLE = 'company_director'` فقط.
  - حُذفت `isPreviewMode` و `previewAs` نهائياً.
  - `fetchAllowedProfile()`: يتحقق من وجود صف المستخدم ودوره؛ أي ملف غير مطابق = تسجيل خروج فوري.
  - رسالة الرفض بالعربية: «هذا التطبيق مخصص للمدير العام فقط. يرجى استخدام تطبيق MedLink على الجوال.»

### 2.4 حذف واجهة مدير الفرع بالكامل
- **حُذف** مجلد `src/pages/branch/` (5 صفحات) و `src/layouts/BranchManagerLayout.tsx`.
- **`src/App.tsx`** — أُزيلت مسارات `/branch/*` بالكامل؛ المسارات المحمية الآن: `/director/*` فقط.

### 2.5 تنظيف الواجهات
- **`src/pages/LoginPage.tsx`** — حُذف قسم وضع المعاينة وزرّا الدخول التجريبي.
- **`src/layouts/DirectorLayout.tsx`** — حُذف شريط المعاينة العلوي.
- **`src/types/models.ts`** — توثيق أن التواريخ أصبحت `timestamptz` (Supabase) بدلاً من Firestore Timestamp.

### 2.6 التشغيل المحلي على Windows
- **`pnpm-workspace.yaml`** — أُزيلت استبعادات ملفات Windows الثنائية (esbuild / rollup / lightningcss / tailwind oxide) كي يعمل Vite على هذا الجهاز.
- **`vite.config.ts`** — دعم `PORT` و `BASE_PATH` من المتغيرات البيئية.

---

## 3. التغييرات التي تمت (المرحلة 2 — إدارة حسابات مدراء الفروع) ⭐ الأولوية القصوى

### 3.1 Edge Function جديدة (في مستودع Flutter — مكان النشر)
- **ملف جديد:** `C:\Users\USER\stitch_medlink_yemen-1\supabase\functions\manage-branch-manager-account\index.ts`
- **النمط:** نسخة معمارية مطابقة لـ `manage-driver-account` لكن:
  - **المتصل:** `company_director` نشط فقط (`role = 'company_director'` و `account_status = 'active'`).
  - **الهدف:** `branch_manager` فقط (يُتحقق من الدور في كل إجراء).
  - **الإجراءات:**
    | الإجراء | الوصف |
    |---|---|
    | `create` | إنشاء حساب مدير فرع (الاسم، البريد، الهاتف، كلمة المرور، `branch_id` مع التحقق من وجود الفرع وحل اسمه) |
    | `update_status` | تفعيل / إيقاف (`active` / `suspended`) |
    | `reset_password` | إعادة تعيين كلمة المرور + إجبار التغيير عند أول دخول |
  - **الحماية:**
    - CORS كامل (preflight + كل الاستجابات).
    - التحقق من JWT عبر `auth.getUser()`.
    - عمليات حساسة عبر service-role فقط.
    - `email_confirm: true`.
    - تأخير 600ms بعد `createUser` كي يكتمل صف الـ trigger قبل التحديث.
    - تراجع تلقائي (حذف الحساب) عند فشل التحديث.
    - `requires_password_change: true` لكل حساب جديد / عند إعادة التعيين.

### 3.2 واجهة الويب
- **`src/lib/branchManagerApi.ts`** (جديد):
  - قراءة مباشرة (مسموحة بـ RLS للمدير العام): `listBranchManagers()` و `listBranches()`.
  - كل الكتابات عبر `functions.invoke('manage-branch-manager-account')`:
    `createBranchManager` / `updateBranchManagerStatus` / `resetBranchManagerPassword`.
  - ترجمة أخطاء Edge Function إلى رسائل عربية.
- **`src/pages/director/BranchManagersPage.tsx`** (جديد):
  - جدول مدراء الفروع: الاسم، البريد، الهاتف، الفرع، شارة الحالة (نشط/موقوف/قيد المراجعة)، شارة «تغيير كلمة المرور مطلوب».
  - بحث وفلترة حسب الحالة.
  - نافذة إنشاء حساب: الاسم + البريد + الهاتف + كلمة المرور + **اختيار الفرع** من جدول `branches`.
  - إيقاف/تفعيل مع نافذة تأكيد.
  - إعادة تعيين كلمة المرور.
- **`src/App.tsx`** — مسار جديد `/director/branch-managers` (خلف حارس `company_director`).
- **`src/layouts/DirectorLayout.tsx`** — عنصر قائمة «مدراء الفروع» + عنوان الصفحة.

### 3.3 تحقق من قواعد RLS (قبل البناء)
| الجدول | العملية | السياسة | النتيجة |
|---|---|---|---|
| `public.users` | SELECT | `users_select_own_or_director` (0001) | ✅ المدير العام يقرأ كل المستخدمين |
| `public.branches` | SELECT | `branches_select_all_authenticated` (0001) | ✅ أي مستخدم مصادق |
| `public.users` | UPDATE | `users_update_own_profile` (0005) | 🚫 لا يعدّل إلا ملفه الشخصي ⇒ **وجوب** Edge Function للكتابة |

---

## 4. حالة التحقق

- ✅ `tsc --noEmit` (بدون أخطاء، exit 0)
- ✅ خادم Vite يعمل: `http://127.0.0.1:5173` (HTTP 200)
- ✅ الصفحات والوحدات الجديدة تتحوَّل بدون أخطاء
- ✅ `.env.local` متجاهَل بـ git

---

## 5. خطوات النشر والتشغيل

### 5.1 نشر Edge Function (من مستودع Flutter)
```bash
cd C:\Users\USER\stitch_medlink_yemen-1
supabase functions deploy manage-branch-manager-account --project-ref lmkomzqioneuyvatzsov
```

### 5.2 تشغيل الويب محلياً
```bash
cd C:\Users\USER\Medlik-Waap
pnpm install
cd artifacts/pharma-pwa
pnpm dev
```
ثم افتح `http://127.0.0.1:5173`.

### 5.3 إنشاء حساب المدير العام (مرة واحدة)
لا يوجد حساب مدير عام مبرمج في المشروع — يُنشأ مباشرة في Supabase (انظر الشرح المرافق).

---

## 6. الأمان

- المفاتيح السرية في `.env.local` فقط (غير مرفوعة إلى git).
- لا كتابة مباشرة على `public.users` من الويب إطلاقاً — كل الكتابات عبر Edge Function + service role.
- سلسلة الأمان: JWT → التحقق من الدور → العمليات الإدارية → إرجاع النتيجة.
- حذف كامل لوضع المعاينة الذي كان يتيح الدخول بحسابات وهمية.
